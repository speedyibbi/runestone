{
  "name": "runestone",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "services/*"
  ],
  "scripts": {
    "build": "npm run build -w @runestone/config && concurrently \"npm run build -w @runestone/server\" \"npm run build -w @runestone/web_app\"",
    "dev": "concurrently \"npm run dev -w @runestone/config\" \"npm run dev -w @runestone/server\" \"npm run dev -w @runestone/web_app\"",
    "lint": "npm run lint --if-present --workspaces",
    "format": "npm run format --if-present --workspaces"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
