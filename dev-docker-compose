services:
  app:
    image: node:22
    container_name: node22
    working_dir: /app
    user: "${LOCAL_UID}:${LOCAL_GID}"
    volumes:
      - .:/app
    env_file:
      - .env
    tty: true
    stdin_open: true
    command: sh -c "npm install && npm run dev"
    ports:
      - "${PORT}:${PORT}"

